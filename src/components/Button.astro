---
export interface Props {
	texto: string;
	iconoSrc: string;
	alt?: string;
}
const { texto, iconoSrc, alt = '' } = Astro.props;
---

<button
	class="flex bg-[#FF0000] items-center rounded-full gap-4 py-3 px-10 text-2xl"
>
	<span class="text-white">{texto}</span>
	<img src={iconoSrc} alt={alt} />
</button>
